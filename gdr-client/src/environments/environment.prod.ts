export const environment = {
  production: true,
  apiurl: 'http://' + window.location.hostname + ":" + window.location.port + '/api/v1',
  wsurl: 'ws://' + window.location.hostname + ":" + window.location.port + '/api/v1/ws'
};
