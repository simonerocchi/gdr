# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Node artifact files
node_modules/
dist/
gdr-client/node_modules/.bin/acorn
gdr-client/node_modules/.bin/acorn
